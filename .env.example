# Database (required)
DATABASE_URL="postgresql://user:password@localhost:5432/modern_church_demo"

# Auth (required — but demo mode bypasses login)
AUTH_SECRET="generate-a-random-secret-here"
NEXTAUTH_URL="http://localhost:3000"
